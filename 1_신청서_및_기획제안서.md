# [Wanted AI Championship 2026] Life Fit (라이프 핏) 프로젝트 제안서

> 이 문서는 critique-me 검증(정직성 렌즈 포함)을 거쳐, 측정하지 않은 수치·실제로 없는 연동을 실제 상태에 맞게 다시 쓴 버전입니다. 제출 전 각 섹션의 [ ] 표시 항목은 실제 배포/검증 후 채워야 합니다.

## 1. 프로젝트 개요

- **프로젝트명**: Life Fit (라이프 핏)
- **한 줄 소개**: 라이프로그(일기·회고) 및 활동 메타데이터에서 추출한 6차원 벡터로 구직자-채용공고 간 "일하는 방식" 적합도를 매칭하는 프로토타입
- **적용 분야**: AI 채용 매칭, 커리어 웰니스, 피플 애널리틱스(HR Tech)
- **개발 상태**: 해커톤 MVP 프로토타입 (아래 "구현 상태" 참고 — 과장 없이 있는 그대로 기재)
- **서비스 접속 링크**: `[ 배포 후 실제 URL 기재 필요 — 미배포 상태로는 제출 불가 ]`

## 2. 기획 배경 및 문제 정의

### ① 기존 채용 매칭의 사각지대
현재 채용 플랫폼의 매칭은 대부분 '이력서/직무 스펙 vs 공고 텍스트' 간 **업무 역량 적합도**에 집중되어 있습니다. 반면 신규 입사자의 조기 퇴사는 역량 부족보다 **일하는 방식의 불일치**(회의 밀도, 몰입 환경, 일-삶 경계)에서 발생하는 경우가 많다는 문제의식에서 출발했습니다.

### ② 기존 솔루션과의 차이 (경쟁 현황 확인 결과)
컬처핏/번아웃 예측 자체는 이미 존재하는 영역입니다 — Crystal Knows, Culture Amp/Cultivate, Predictive Index, MyCulture.ai 등 글로벌 HR테크와 국내 사람인 "AI 커리어 매칭 에이전트", 잡코리아 AI 매칭이 이미 움직이고 있습니다. Life Fit이 다른 지점은 **자기보고식 설문이 아니라, 일기·캘린더·슬랙 같은 수동적(passive) 행동 신호에서 6축을 추론**한다는 접근입니다. "세상에 없던 개념"이 아니라 "설문 기반 대비 수동 신호 기반"이라는 좁고 구체적인 차별점으로 제시합니다.

### ③ 원티드 비즈니스와의 연결 (검증 필요 전제)
원티드의 채용 수수료가 일정 근속 기간을 조건으로 정산/환불되는 구조라는 점([원티드 채용 수수료 안내](https://help.wanted.co.kr/hc/ko/sections/27392520990361-%EC%B1%84%EC%9A%A9-%EC%88%98%EC%88%98%EB%A3%8C))에 착안해, 조기 퇴사를 줄이는 매칭이 곧 원티드의 수수료 리스크 방어로 이어질 수 있다는 가설을 세웠습니다. **정확한 근속 조건·환불 비율은 원티드 내부 정책 확인이 필요하며, 아래 ROI 수치는 이 가설에 기반한 예시 계산입니다.**

## 3. 핵심 솔루션 및 기능

### ① Two-Stage 파이프라인 (실제 구현 상태)
1. **Stage 1 (PII 마스킹)**: 정규식 기반으로 전화번호·이메일·주민등록번호 형식·특정 관계어 패턴을 마스킹. 완전한 PII 탐지는 아니며, 서버가 원본 텍스트를 별도로 저장/로깅하지 않는 구조(무상태 처리)로 "제로 리텐션"을 뒷받침.
2. **Stage 2 (벡터 추출)**: `GEMINI_API_KEY`(무료 티어, 카드 등록 불필요)가 있으면 최우선으로 Gemini를 호출합니다(`engine: "llm_gemini"`). 없으면 `OPENAI_API_KEY`(유료)를 시도하고(`engine: "llm_openai"`), 둘 다 없거나 호출이 실패하면 키워드 기반 규칙 로직으로 폴백합니다(`engine: "rule_based_fallback"`). API 응답의 `engine` 필드로 실제 어떤 경로였는지 항상 확인 가능합니다.

### ② 6차원 Life-Work 벡터 공간
- $d_1$ Autonomy(자율성) · $d_2$ Deep Work(몰입시간) · $d_3$ Social Load(소통부하) · $d_4$ Pace Predictability(루틴예측성) · $d_5$ Async Collaboration(비동기협업) · $d_6$ Boundary Integrity(경계보호)

### ③ 사용자 주도 검증
AI 추론 결과를 레이더 차트 + 슬라이더 UI로 직접 확인·수정 가능. 자동 추론을 강제하지 않고 사용자가 최종 벡터를 통제합니다.

### ④ 제로 인풋 프로토타입 (Phase 2, 시뮬레이션)
구글 캘린더/슬랙 원클릭 버튼은 **실제 API 연동이 아니라, 연동 시 나올 법한 샘플 데이터로 UX 흐름을 시연하는 프로토타입**입니다. 데모 화면과 문서 모두에 이 사실을 명시했습니다. "구현 완료"가 아니라 "UX 프로토타입 완료, 실제 연동은 Phase 2 다음 단계"로 정확히 기재합니다.

## 4. 기술적 구현 (Architecture)

### ① 매칭 수식 (Cosine Similarity + Weighted Distance + 페널티)
$$\text{Fit Score} = \left( 0.40 \times \text{CosineSim}(U, J) + 0.60 \times \left(1 - \frac{\sum_i |U_i - J_i|}{6}\right) \right) \times 100 \times (1 - \text{Penalty})$$
- 구직자의 Social Load가 0.75 초과이고 공고의 Social Load가 0.80 초과일 때 12% 감점 페널티 적용.

### ② 참고 지표(예시 공식) — "예측 모델"이 아님을 명확히 함
$$\text{Illustrative Retention Rate (\%)} = \min(98.0,\ \max(42.0,\ 36.0 + 0.62 \times \text{Fit Score}))$$
이 공식은 **실제 근속 데이터로 피팅되지 않은 예시 스코어링**입니다. 신청서·발표 어디에서도 "검증된 예측 모델"이라고 표현하지 않으며, API 응답에도 `"illustrativeRetentionRate"`와 함께 이 사실을 명시하는 `note` 필드를 포함합니다. 실제 모델화하려면 근속 여부가 라벨링된 실측 데이터셋이 필요합니다(현재 없음).

## 5. 평가 계획 및 비즈니스 임팩트 (가정 명시)

### ① 정량 평가 — 계획 (아직 측정 안 됨)
- Signal Precision, Dimension F1-Score 등은 **아직 측정하지 않았습니다.** 소규모 라벨링 평가셋(N=50 내외) 구축 후 측정할 계획이며, 측정 전까지 신청서에 구체적 수치를 기재하지 않습니다.
- Zero-Retention은 "서버에 원본 텍스트를 저장하는 코드가 없다"는 구조적 사실이며, 별도 컴플라이언스 인증을 받은 것은 아닙니다.

### ② 비즈니스 임팩트 (가정 기반 예시 계산, 실측 아님)
- 가정: 조기퇴사율 16.8%→6.5% 개선(10.3%p), 채용 1,000건, 건당 평균 수수료 약 420만원 — **이 세 숫자 모두 원티드 실데이터가 아닌 가정치**입니다.
- 가정하에 계산 시 연간 약 4.3억원 규모의 수수료 리스크 방어 효과가 있을 수 있다는 **시나리오**를 제시합니다. 실제 수치 검증에는 원티드 측 조기퇴사율·수수료 데이터가 필요합니다.

## 6. 향후 발전 계획 (Roadmap)

- **Phase 1 (완료)**: 텍스트 기반 규칙 파서 + LLM 연동 가능한 구조 + 6축 매칭 데모, 배포용 코드 정리.
- **Phase 2 (다음 단계, 현재는 UX 시뮬레이션만 완료)**: 실제 Google Calendar API / Slack API OAuth 연동, 실측 평가셋으로 정확도 측정.
- **Phase 3**: 기업용 피플 애널리틱스 대시보드, 실제 근속 데이터 기반 리텐션 모델 재학습.

## 7. 개발 스택
Python (FastAPI), LLM 연동 구조(Gemini API 우선 · 무료 티어 / OpenAI API 폴백 · 유료, 키 설정 시 활성화, 미설정 시 규칙 기반 폴백), HTML5/TailwindCSS/Chart.js, Vector Space Modeling
